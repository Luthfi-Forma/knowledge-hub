# Technical Debt — knowledge-hub

<!-- APPEND-ONLY register. Add a row the moment a shortcut is consciously
     taken (CLAUDE.md, Session protocol) — not weeks later from memory.
     Severity: high = risks correctness/security, med = slows work,
     low = cosmetic. Close by filling "Closed by" (commit or ADR), keep the row. -->

| # | Date | Debt item | Severity | Why taken | Cost to fix | Closed by |
|---|---|---|---|---|---|---|
| 1 | 2026-07-17 | Pagefind search UI (`/explore`) uses its default CSS, not the site's design tokens (zinc palette, Plus Jakarta Sans/Manrope) | low | T-12 scoped explicitly to "search works," not visual polish — restyling was flagged in-prompt as a possible follow-up, not attempted | Small — override `pagefind-ui.css` custom properties or restyle the component UI | T-60 (2026-07-30) |
| 2 | 2026-07-17 | Project titles on `/projects` use a manual override map (`getProjectTitle` in `src/lib/posts.ts`) for names that don't naively title-case correctly (e.g. "CDMP-Jabodetabek"), instead of a general solution | low | Only 3 real projects exist; a hardcoded map was simpler and more honest than a fragile acronym-detection heuristic | Small per new project (add one map entry) — revisit if the project list grows past a handful and this becomes tedious | |
| 3 | 2026-08-04 | 3 Tanggamus photo source files (`tanggamus-boat/pier/wave-cover.jpg`) are 15-20MB each (~55MB total) in `src/content/posts/` — Astro re-optimizes them into responsive `.webp` at build time, but the oversized source still costs repo weight and build time | low | Found in passing during T-66 (cover art SOP work) while measuring cover geometry; outside that task's scope (T-66 is about generated project covers, not photo posts) — `docs/design/COVER_ART.md` §8 now sets a ~5MB source-file ceiling going forward, but resizing these 3 existing files is a separate task | Small-medium — resize/re-compress the 3 source JPGs (e.g. cap the long edge, re-export at a sane JPEG quality) and re-verify the photo pages still render correctly | |
| 4 | 2026-08-09 | Several M10 modules sit outside the build graph, so `astro build` never typechecks them: `src/islands/StoryStage.tsx` and its imports (`useStoryProgress.ts`, `story/types.ts`), plus `story/viz/Chart.tsx`, `DataTable.tsx`, `Legend.tsx`, and `story/geo/project.ts` (T-77 — its test exercises it at runtime, which catches logic errors but not type errors). Vite only compiles reachable modules and this project has no TypeScript installed, so an error in these files surfaces at no point in the normal workflow | med | Framework built additively ahead of its first real consumer, per ADR-005's incremental migration (the four live posts must keep working while it lands). Every one of these files WAS compiled and verified during T-74/T-76/T-77 via temporary probe pages, so they are known-good as of this commit — the gap is that future edits go unchecked until T-78 wires the real Cikarang stage | Small: add `@astrojs/check` + `typescript` as devDependencies and put `astro check` in the `build` script, which typechecks the whole `src/` tree regardless of reachability. Two new devDeps — worth raising with the owner because ADR-005 sets a zero-new-dependency target for M10 (aimed at runtime deps, but the line should be drawn deliberately rather than assumed). Alternatively it closes on its own at T-78 | |
| 5 | 2026-08-09 | **Angka yang bertentangan di post yang sudah tayang** (`cikarang-industrial-settlement-pattern`). Scene "Finding 01" menyatakan "rose from 4,477.99 to 5,570.68 hectares, an addition of 687.45 hectares" — selisih kedua endpoint itu **+1.092,69**, bukan 687,45. Sama untuk residensial: "grew by 622.9 hectares ... from 6,236.05 to 7,328.73" = **+1.092,68**. Panel yang sama merender keduanya berdampingan: bar chart dari `TOTAL_LAND` (4.477,99→5.570,68) dan `AnimatedNumber` `+687,45 ha`. Dua fakta terkait: (a) absolut 2023 per distrik berjumlah 5.166,43 / 6.859,34 — meleset 404,25 / 469,39 ha dari total yang dinyatakan; (b) jumlah delta per distrik (687,44 / 622,91) cocok dengan prosa tapi tidak dengan selisih `TOTAL_LAND`. Kedua selisih endpoint identik (1.092,69 dan 1.092,68), pola yang biasanya muncul saat satu angka diturunkan dengan mengurangi konstanta yang sama. **Hipotesis yang belum terbukti**: tabel distrik mungkin mencakup 5 kecamatan Cikarang saja sementara `TOTAL_LAND` mencakup wilayah lebih luas — kalau benar, semua angkanya sah dan yang salah hanya kalimat yang mencampur dua cakupan. Tidak bisa dipastikan dari repo | high | Ditemukan saat T-78 memverifikasi aritmetika sebelum membangun scene spasial di atas data ini. Tidak diperbaiki menebak-nebak: angka mana yang benar ada di paper Rahman & Hernanda (2025) Tabel 1, bukan di repo, dan mendeduksi baseline 2016 dari data yang belum rekonsiliasi akan melipatgandakan kesalahan yang belum dipahami ke dalam sebuah peta | Kecil setelah datanya jelas — perlu pemilik situs membuka Tabel 1 dan memberi: total industri/residensial 2016 dan 2023, apakah 5 distrik itu seluruh wilayah studi atau sebagian, dan nilai 2016 per distrik kalau ada. Lalu perbaiki prosa dan/atau `TOTAL_LAND`/`LAND_BY_DISTRICT_2023` agar konsisten | |
