---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'posts'>;
  href?: string;
}

const { post, href } = Astro.props;
const { title, summary, cover, date, type } = post.data;
const formattedDate = date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
const typeLabel = type.charAt(0).toUpperCase() + type.slice(1);
---

<article class="group">
  <a href={href ?? undefined} aria-disabled={!href} class="block focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent">
    {
      cover ? (
        <Image
          src={cover}
          alt=""
          class="aspect-4/3 w-full rounded-lg object-cover"
          widths={[400, 800]}
          sizes="(min-width: 768px) 388px, 100vw"
        />
      ) : (
        <div class="flex aspect-4/3 w-full items-center justify-center rounded-lg bg-line text-xs text-ink-muted">
          {typeLabel}
        </div>
      )
    }
    <h3 class="mt-3 text-[0.9375rem] font-semibold text-ink group-hover:text-accent">{title}</h3>
    <p class="mt-1 text-sm leading-normal text-ink-muted">{summary}</p>
    <p class="mt-1 text-xs text-ink-muted">— {formattedDate} · {typeLabel}</p>
  </a>
</article>
